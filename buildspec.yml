version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.9

  build:
    commands:
      - pip install aws-sam-cli
      - sam build

  post_build:
    commands:
      - sam deploy --no-confirm-changeset --no-fail-on-empty-changeset

artifacts:
  files:
    - '*/'
